---
import Base from '../layouts/Base.astro';
import TopBar from '../components/TopBar.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import StickyBar from '../components/StickyBar.astro';
import SeasonBanner from '../components/SeasonBanner.astro';
import { Image } from 'astro:assets';
import { tourPackages, fmt } from '../data/pricing';

import CharDhamImg   from '../assets/images/CharDham1.jpeg';
import NainitalImg   from '../assets/images/Nainital2.jpg';
import AuliImg       from '../assets/images/Auli_togo.jpg';
import KedarnathImg  from '../assets/images/CharDham2.jpeg';
import BadrinathImg  from '../assets/images/Badrinath.jpeg';
import NeemKaroliImg from '../assets/images/NeemKaroli.jpeg';
import DhanaultiImg  from '../assets/images/Dhanaulti.jpeg';
import MunsiyariImg  from '../assets/images/Munsyari.jpeg';
import ChoptaImg     from '../assets/images/Chopta.jpeg';

const phone = import.meta.env.PUBLIC_BUSINESS_PHONE || '+916397321825';
const wa    = import.meta.env.PUBLIC_WHATSAPP_NUMBER || '916397321825';

const imgMap: Record<string, ImageMetadata> = {
  'dhanaulti':   DhanaultiImg,
  'munsiyari':   MunsiyariImg,
  'chopta':      ChoptaImg,
  'neem-karoli': NeemKaroliImg,
  'nainital':    NainitalImg,
  'auli':        AuliImg,
  'kedarnath':   KedarnathImg,
  'badrinath':   BadrinathImg,
  'ek-dham':     CharDhamImg,
  'do-dham':     CharDhamImg,
  'char-dham':   CharDhamImg,
};

// Filter packages by section
const dayPackages = tourPackages.filter(p =>
  ['dhanaulti', 'neem-karoli', 'nainital', 'auli', 'munsiyari', 'chopta', 'kedarnath', 'badrinath'].includes(p.id)
);
const charDhamPackages = tourPackages.filter(p =>
  ['ek-dham', 'do-dham', 'char-dham'].includes(p.id)
);
const multiDestPackages = tourPackages.filter(p =>
  ['nainital-corbett', 'mussoorie-corbett-nainital-ranikhet', 'nainital-ranikhet-corbett', 'almora-kausani'].includes(p.id)
);

const schema = {
  '@context': 'https://schema.org',
  '@type': 'ItemList',
  name: 'Uttarakhand Tour Packages with Cab from Dehradun',
  description: 'Curated cab packages for Char Dham Yatra, Nainital, Auli, Kedarnath, Badrinath, Neem Karoli Baba and more from Dehradun',
  itemListElement: tourPackages.map((p, i) => ({
    '@type': 'ListItem',
    position: i + 1,
    name: p.name,
    url: `https://shritaxi.com${p.href}`,
    description: p.seoDesc,
  })),
};
---

<Base
  title="Uttarakhand Tour Packages with Cab | Char Dham, Nainital, Auli | Shri Taxi"
  description="Book Uttarakhand tour packages with cab from Dehradun. Char Dham Yatra, Nainital, Auli, Kedarnath, Badrinath, Neem Karoli Baba. All inclusive pricing. 10% discount."
  canonicalUrl="https://shritaxi.com/packages"
>
  <script type="application/ld+json" is:inline set:html={JSON.stringify(schema)} slot="head" />

  <TopBar />
  <SeasonBanner />
  <Header currentPath="/packages" />

  <main id="main-content">

    <!-- Hero -->
    <section class="pkg-hero">
      <div class="container">
        <nav class="breadcrumb" aria-label="Breadcrumb">
          <a href="/">Home</a> › <span>Tour Packages</span>
        </nav>
        <h1 class="pkg-hero-title">Uttarakhand Tour Packages with Cab</h1>
        <p class="pkg-hero-sub">All inclusive · Experienced hill drivers · 10% discount on all packages · Driver + fuel + toll + parking included</p>
        <div class="pkg-hero-ctas">
          <a href={`tel:${phone}`} class="btn btn-call">📞 Call Now</a>
          <a href={`https://wa.me/${wa}?text=Hi%20Shri%20Taxi%2C%20I%20want%20to%20know%20about%20your%20tour%20packages`} target="_blank" rel="noopener" class="btn btn-whatsapp">💬 WhatsApp Us</a>
        </div>
      </div>
    </section>

    <!-- Discount announcement -->
    <div class="discount-bar">
      <div class="container discount-bar-inner">
        <span class="discount-fire">🔥</span>
        <p><strong>Limited Time — 10% OFF on all packages!</strong> Book now and save up to ₹8,991 on Char Dham Yatra</p>
        <span class="discount-badge">10% OFF</span>
      </div>
    </div>

    <!-- Category Chip Filter -->
    <div class="pkg-filter-bar">
      <div class="container">
        <p class="pkg-filter-label">What are you looking for?</p>
        <div class="pkg-chips">
          <button class="pkg-chip pkg-chip--active" data-filter="all">🌿 All Packages</button>
          <button class="pkg-chip" data-filter="day">🏔️ Day Trips</button>
          <button class="pkg-chip" data-filter="chardham">🙏 Char Dham</button>
          <button class="pkg-chip pkg-chip--highlight" data-filter="circuits">🏙️ Delhi & Chandigarh <span class="chip-new">NEW</span></button>
        </div>
      </div>
    </div>

    <!-- Day/Short Packages — Accordion -->
    <section class="section pkg-sections">
      <div class="container">

    <details class="pkg-accordion" id="day-packages" data-category="day" open>
      <summary class="pkg-accordion-header">
        <div class="pkg-accordion-title">
          <span class="pkg-accordion-icon">🏔️</span>
          <div>
            <h2>Day Trips & Short Packages</h2>
            <p>From Dehradun · Starting ₹3,599</p>
          </div>
        </div>
        <div class="pkg-accordion-meta">
          <span class="pkg-accordion-count">{dayPackages.length} packages</span>
          <svg class="pkg-accordion-chevron" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><polyline points="6 9 12 15 18 9"/></svg>
        </div>
      </summary>
      <div class="pkg-accordion-body">
        <div class="pkg-grid">
          {dayPackages.map(p => {
            const img = imgMap[p.id] || CharDhamImg;
            return (
              <div class="pkg-card" id={p.id}>
                <div class="pkg-img-wrap">
                  <Image src={img} alt={`${p.name} taxi package from Dehradun`} width={400} height={220} class="pkg-img" loading="lazy" format="webp" />
                  <div class="pkg-overlay"></div>
                  <span class="pkg-badge" style={`background:${p.badgeColor}`}>{p.tag}</span>
                  <span class="pkg-nights">{p.nights > 0 ? `${p.nights}N ` : ''}{p.days}D</span>
                  <div class="pkg-discount-circle"><span>10%</span><span>OFF</span></div>
                </div>
                <div class="pkg-body">
                  <h3 class="pkg-name">{p.name}</h3>
                  <div class="pkg-highlights">
                    {p.highlights.map(h => <span class="pkg-highlight-chip">{h}</span>)}
                  </div>
                  <div class="pkg-pricing-table">
                    <div class="pkg-pricing-header">
                      <span>Vehicle</span><span>MRP</span><span>You Pay</span><span>Save</span>
                    </div>
                    {p.tiers.map(t => (
                      <div class="pkg-pricing-row">
                        <span class="pkg-tier-vehicle">{t.vehicleId === 'sedan' ? '🚗' : t.vehicleId === 'ertiga' ? '🚐' : t.vehicleId === 'innova' ? '🚙' : '🚌'} {t.vehicleId === 'sedan' ? 'Sedan' : t.vehicleId === 'ertiga' ? 'Ertiga' : t.vehicleId === 'innova' ? 'Innova Crysta' : 'Traveller'}</span>
                        <span class="pkg-tier-mrp">{fmt(t.mrp)}</span>
                        <span class="pkg-tier-final">{fmt(t.discounted)}</span>
                        <span class="pkg-tier-save">-{fmt(t.saving)}</span>
                      </div>
                    ))}
                  </div>
                  <p class="pkg-inclusive">✓ All inclusive — driver, fuel, toll & parking</p>
                  <div class="pkg-actions">
                    <a href={`https://wa.me/${wa}?text=Hi%20Shri%20Taxi%2C%20I%27m%20interested%20in%20${encodeURIComponent(p.name)}%20package`} target="_blank" rel="noopener" class="btn btn-whatsapp pkg-btn">💬 Book Now</a>
                    <a href={`tel:${phone}`} class="btn btn-outline pkg-btn">📞 Call Us</a>
                  </div>
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </details>

    <!-- Char Dham Packages — Accordion -->
    <details class="pkg-accordion" id="char-dham-packages" data-category="chardham">
      <summary class="pkg-accordion-header">
        <div class="pkg-accordion-title">
          <span class="pkg-accordion-icon">🙏</span>
          <div>
            <h2>Char Dham Yatra 2026</h2>
            <p>From Dehradun · Season Open · Starting ₹10,999</p>
          </div>
        </div>
        <div class="pkg-accordion-meta">
          <span class="pkg-accordion-count">{charDhamPackages.length} packages</span>
          <svg class="pkg-accordion-chevron" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><polyline points="6 9 12 15 18 9"/></svg>
        </div>
      </summary>
      <div class="pkg-accordion-body">
        <div class="chardham-grid">
          {charDhamPackages.map(p => (
            <div class="chardham-card" id={p.id}>
              <div class="chardham-header">
                <div class="chardham-header-left">
                  <div class="chardham-icon">{p.id === 'ek-dham' ? '🙏' : p.id === 'do-dham' ? '🙏🙏' : '🙏🙏🙏🙏'}</div>
                  <div>
                    <span class="chardham-tag" style={`background:${p.badgeColor}`}>{p.id === 'ek-dham' ? 'Ek Dham' : p.id === 'do-dham' ? 'Do Dham' : 'Char Dham'} · {p.nights}N {p.days}D</span>
                    <h3 class="chardham-name">{p.name}</h3>
                    <p class="chardham-sub">{p.id === 'ek-dham' ? 'Choose any 1 Dham — Kedarnath, Badrinath, Yamunotri or Gangotri' : p.id === 'do-dham' ? 'Kedarnath + Badrinath · OR Yamunotri + Gangotri' : 'Yamunotri · Gangotri · Kedarnath · Badrinath — All 4 Dhams'}</p>
                  </div>
                </div>
                <div class="chardham-discount-badge">10% OFF</div>
              </div>
              <div class="pkg-highlights" style="margin-bottom:14px">
                {p.highlights.map(h => <span class="pkg-highlight-chip">{h}</span>)}
              </div>
              {p.itinerary && (
                <div class="chardham-itinerary">
                  {p.itinerary.map(day => <p class="itinerary-day">📍 {day}</p>)}
                </div>
              )}
              <div class="chardham-pricing-table">
                {p.tiers.map(t => (
                  <div class="chardham-tier">
                    <span class="chardham-tier-vehicle">
                      {t.vehicleId === 'sedan' ? '🚗' : t.vehicleId === 'ertiga' ? '🚐' : t.vehicleId === 'innova' ? '🚙' : '🚌'}
                      {t.vehicleId === 'sedan' ? 'Sedan' : t.vehicleId === 'ertiga' ? 'Ertiga' : t.vehicleId === 'innova' ? 'Innova Crysta' : 'Traveller'}
                      <span class="chardham-seats">({t.seats}+1)</span>
                    </span>
                    <div class="chardham-tier-price">
                      <span class="chardham-mrp">{fmt(t.mrp)}</span>
                      <span class="chardham-final">{fmt(t.discounted)}</span>
                      <span class="chardham-save">Save {fmt(t.saving)}</span>
                    </div>
                  </div>
                ))}
              </div>
              <p class="pkg-inclusive">✓ All inclusive — driver, fuel, toll & parking</p>
              <div class="pkg-actions">
                <a href={`https://wa.me/${wa}?text=Hi%20Shri%20Taxi%2C%20I%27m%20interested%20in%20${encodeURIComponent(p.name)}%202026`} target="_blank" rel="noopener" class="btn btn-whatsapp pkg-btn">💬 Book Now</a>
                <a href="/char-dham-yatra" class="btn btn-outline pkg-btn">View Details</a>
              </div>
            </div>
          ))}
        </div>
      </div>
    </details>

    <!-- Delhi & Chandigarh Circuits — Accordion -->
    <details class="pkg-accordion pkg-accordion--highlight" id="multi-dest-packages" data-category="circuits">
      <summary class="pkg-accordion-header">
        <div class="pkg-accordion-title">
          <span class="pkg-accordion-icon">🏙️</span>
          <div>
            <h2>Delhi & Chandigarh Circuit Tours <span class="accordion-new-badge">NEW</span></h2>
            <p>From Delhi NCR & Chandigarh · Same price · Starting ₹17,999</p>
          </div>
        </div>
        <div class="pkg-accordion-meta">
          <span class="pkg-accordion-count">{multiDestPackages.length} packages</span>
          <svg class="pkg-accordion-chevron" width="20" height="20" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5"><polyline points="6 9 12 15 18 9"/></svg>
        </div>
      </summary>
      <div class="pkg-accordion-body">
        <div class="multi-notice">
          <span>📍</span>
          <p><strong>Same price from Delhi NCR & Chandigarh.</strong> Cab only — hotel, meals & safari not included.</p>
        </div>
        <div class="multi-grid">
          {multiDestPackages.map(p => (
            <div class="multi-card" id={p.id}>
              <div class="multi-header">
                <div>
                  <span class="multi-tag" style={`background:${p.badgeColor}`}>{p.tag}</span>
                  <h3 class="multi-name">{p.name}</h3>
                  <p class="multi-duration">⏱ {p.nights}N {p.days}D</p>
                </div>
                <div class="multi-discount">10% OFF</div>
              </div>
              <div class="multi-highlights">
                {p.highlights.map(h => <span class="multi-highlight">{h}</span>)}
              </div>
              {p.itinerary && (
                <div class="multi-itinerary">
                  {p.itinerary.map(day => <div class="multi-day">📍 {day}</div>)}
                </div>
              )}
              <div class="multi-pricing-table">
                {p.tiers.map(t => (
                  <div class="multi-tier">
                    <span class="multi-tier-vehicle">
                      {t.vehicleId === 'sedan' ? '🚗' : t.vehicleId === 'ertiga' ? '🚐' : t.vehicleId === 'innova' ? '🚙' : '🚌'}
                      {t.vehicleId === 'sedan' ? 'Sedan' : t.vehicleId === 'ertiga' ? 'Ertiga' : t.vehicleId === 'innova' ? 'Innova Crysta' : 'Traveller'}
                      <span class="multi-tier-seats">({t.seats}+1)</span>
                    </span>
                    <div class="multi-tier-price">
                      <span class="multi-mrp">{fmt(t.mrp)}</span>
                      <span class="multi-final">{fmt(t.discounted)}</span>
                      <span class="multi-save">Save {fmt(t.saving)}</span>
                    </div>
                  </div>
                ))}
              </div>
              <p class="multi-inclusive">✓ Cab — driver, fuel, toll & parking included</p>
              {p.excludes && p.excludes.length > 0 && (
                <details class="multi-exclusions">
                  <summary class="multi-excl-summary">View Exclusions ▾</summary>
                  <ul class="multi-excl-list">
                    {p.excludes.map(e => <li>✗ {e}</li>)}
                  </ul>
                </details>
              )}
              <div class="multi-actions">
                <a href={`https://wa.me/${wa}?text=Hi%20Shri%20Taxi%2C%20I%27m%20interested%20in%20${encodeURIComponent(p.name)}%20package`} target="_blank" rel="noopener" class="btn btn-whatsapp pkg-btn">💬 Book Now</a>
                <a href={`tel:${phone}`} class="btn btn-outline pkg-btn">📞 Call Us</a>
              </div>
            </div>
          ))}
        </div>
      </div>
    </details>
      <div class="container">
        <p class="section-label">Day Trips & Short Packages</p>
        <h2 class="section-title">Popular Tour Packages from Dehradun</h2>
        <p class="section-sub">Curated cab packages for spiritual pilgrimages, adventure trips and cultural experiences</p>

        <div class="pkg-grid">
          {dayPackages.map(p => {
            const img = imgMap[p.id] || CharDhamImg;
            return (
              <div class="pkg-card" id={p.id}>
                <!-- Image -->
                <div class="pkg-img-wrap">
                  <Image
                    src={img}
                    alt={`${p.name} taxi package from Dehradun`}
                    width={400}
                    height={220}
                    class="pkg-img"
                    loading="lazy"
                  />
                  <div class="pkg-overlay"></div>
                  <span class="pkg-badge" style={`background:${p.badgeColor}`}>{p.tag}</span>
                  <span class="pkg-nights">{p.nights > 0 ? `${p.nights}N ` : ''}{p.days}D</span>
                  <div class="pkg-discount-circle">
                    <span>10%</span>
                    <span>OFF</span>
                  </div>
                </div>

                <!-- Body -->
                <div class="pkg-body">
                  <h3 class="pkg-name">{p.name}</h3>
                  <div class="pkg-highlights">
                    {p.highlights.map(h => <span class="pkg-highlight-chip">{h}</span>)}
                  </div>

                  <!-- Pricing table -->
                  <div class="pkg-pricing-table">
                    <div class="pkg-pricing-header">
                      <span>Vehicle</span>
                      <span>MRP</span>
                      <span>You Pay</span>
                      <span>Save</span>
                    </div>
                    {p.tiers.map(t => (
                      <div class="pkg-pricing-row">
                        <span class="pkg-tier-vehicle">{t.vehicleId === 'sedan' ? '🚗' : t.vehicleId === 'ertiga' ? '🚐' : t.vehicleId === 'innova' ? '🚙' : '🚌'} {t.vehicleId === 'sedan' ? 'Sedan' : t.vehicleId === 'ertiga' ? 'Ertiga' : t.vehicleId === 'innova' ? 'Innova Crysta' : 'Traveller'}</span>
                        <span class="pkg-tier-mrp">{fmt(t.mrp)}</span>
                        <span class="pkg-tier-final">{fmt(t.discounted)}</span>
                        <span class="pkg-tier-save">-{fmt(t.saving)}</span>
                      </div>
                    ))}
                  </div>

                  <p class="pkg-inclusive">✓ All inclusive — driver, fuel, toll & parking</p>

                  <!-- Actions -->
                  <div class="pkg-actions">
                    <a
                      href={`https://wa.me/${wa}?text=Hi%20Shri%20Taxi%2C%20I%27m%20interested%20in%20${encodeURIComponent(p.name)}%20package`}
                      target="_blank"
                      rel="noopener"
                      class="btn btn-whatsapp pkg-btn"
                    >
                      💬 Book Now
                    </a>
                    <a href={`tel:${phone}`} class="btn btn-outline pkg-btn">
                      📞 Call Us
                    </a>
                  </div>
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </section>

    <!-- Char Dham Packages -->
    <section class="section section--mint" id="char-dham-packages">
      <div class="container">
        <p class="section-label">Char Dham Yatra 2026</p>
        <h2 class="section-title">Char Dham Yatra Packages from Dehradun</h2>
        <p class="section-sub">Complete Char Dham Yatra taxi packages — Yamunotri, Gangotri, Kedarnath, Badrinath. Season open 2026.</p>

        <div class="chardham-grid">
          {charDhamPackages.map(p => (
            <div class="chardham-card" id={p.id}>
              <div class="chardham-header">
                <div class="chardham-header-left">
                  <div class="chardham-icon">
                    {p.id === 'ek-dham' ? '🙏' : p.id === 'do-dham' ? '🙏🙏' : '🙏🙏🙏🙏'}
                  </div>
                  <div>
                    <span class="chardham-tag" style={`background:${p.badgeColor}`}>
                      {p.id === 'ek-dham' ? 'Ek Dham' : p.id === 'do-dham' ? 'Do Dham' : 'Char Dham'} · {p.nights}N {p.days}D
                    </span>
                    <h3 class="chardham-name">{p.name}</h3>
                    <p class="chardham-sub">
                      {p.id === 'ek-dham' ? 'Choose any 1 Dham — Kedarnath, Badrinath, Yamunotri or Gangotri'
                        : p.id === 'do-dham' ? 'Kedarnath + Badrinath · OR Yamunotri + Gangotri'
                        : 'Yamunotri · Gangotri · Kedarnath · Badrinath — All 4 Dhams'}
                    </p>
                  </div>
                </div>
                <div class="chardham-discount-badge">10% OFF</div>
              </div>

              <div class="pkg-highlights" style="margin-bottom:14px">
                {p.highlights.map(h => <span class="pkg-highlight-chip">{h}</span>)}
              </div>

              {p.itinerary && (
                <div class="chardham-itinerary">
                  {p.itinerary.map(day => <p class="itinerary-day">📍 {day}</p>)}
                </div>
              )}

              <div class="chardham-pricing-table">
                {p.tiers.map(t => (
                  <div class="chardham-tier">
                    <span class="chardham-tier-vehicle">
                      {t.vehicleId === 'sedan' ? '🚗' : t.vehicleId === 'ertiga' ? '🚐' : t.vehicleId === 'innova' ? '🚙' : '🚌'}
                      {t.vehicleId === 'sedan' ? 'Sedan' : t.vehicleId === 'ertiga' ? 'Ertiga' : t.vehicleId === 'innova' ? 'Innova Crysta' : 'Traveller'}
                      <span class="chardham-seats">({t.seats}+1)</span>
                    </span>
                    <div class="chardham-tier-price">
                      <span class="chardham-mrp">{fmt(t.mrp)}</span>
                      <span class="chardham-final">{fmt(t.discounted)}</span>
                      <span class="chardham-save">Save {fmt(t.saving)}</span>
                    </div>
                  </div>
                ))}
              </div>

              <p class="pkg-inclusive">✓ All inclusive — driver, fuel, toll & parking</p>
              <div class="pkg-actions">
                <a href={`https://wa.me/${wa}?text=Hi%20Shri%20Taxi%2C%20I%27m%20interested%20in%20${encodeURIComponent(p.name)}%202026`} target="_blank" rel="noopener" class="btn btn-whatsapp pkg-btn">💬 Book Now</a>
                <a href="/char-dham-yatra" class="btn btn-outline pkg-btn">View Details</a>
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Multi-Destination Packages -->
    <section class="section" id="multi-dest-packages">
      <div class="container">
        <p class="section-label">Multi-Destination Tours</p>
        <h2 class="section-title">Uttarakhand Circuit Tour Packages</h2>
        <p class="section-sub">Complete Uttarakhand circuits from Delhi, Chandigarh & Dehradun — cab only, transparent pricing</p>

        <div class="multi-notice">
          <span>🏙️</span>
          <p><strong>Pickup from Delhi, Chandigarh or Dehradun</strong> — same price for all cities.</p>
        </div>

        <div class="cab-only-notice">
          <span>ℹ️</span>
          <p><strong>Cab-Only Package</strong> — Includes cab with driver, fuel, toll and parking only. Hotel, meals, safari permits and entrance fees are not included.</p>
        </div>

        <div class="multi-grid">
          {multiDestPackages.map(p => (
            <div class="multi-card" id={p.id}>
              <div class="multi-header">
                <div>
                  <span class="multi-tag" style={`background:${p.badgeColor}`}>{p.tag}</span>
                  <h3 class="multi-name">{p.name}</h3>
                  <p class="multi-duration">⏱ {p.nights}N {p.days}D</p>
                </div>
                <div class="multi-discount">10% OFF</div>
              </div>
              <div class="multi-highlights">
                {p.highlights.map(h => <span class="multi-highlight">{h}</span>)}
              </div>
              {p.itinerary && (
                <div class="multi-itinerary">
                  {p.itinerary.map(day => <div class="multi-day">📍 {day}</div>)}
                </div>
              )}
              <div class="multi-pricing-table">
                {p.tiers.map(t => (
                  <div class="multi-tier">
                    <span class="multi-tier-vehicle">
                      {t.vehicleId === 'sedan' ? '🚗' : t.vehicleId === 'ertiga' ? '🚐' : t.vehicleId === 'innova' ? '🚙' : '🚌'}
                      {t.vehicleId === 'sedan' ? 'Sedan' : t.vehicleId === 'ertiga' ? 'Ertiga' : t.vehicleId === 'innova' ? 'Innova Crysta' : 'Traveller'}
                      <span class="multi-tier-seats">({t.seats}+1)</span>
                    </span>
                    <div class="multi-tier-price">
                      <span class="multi-mrp">{fmt(t.mrp)}</span>
                      <span class="multi-final">{fmt(t.discounted)}</span>
                      <span class="multi-save">Save {fmt(t.saving)}</span>
                    </div>
                  </div>
                ))}
              </div>
              <p class="multi-inclusive">✓ Cab inclusive — driver, fuel, toll & parking</p>
              {p.excludes && p.excludes.length > 0 && (
                <details class="multi-exclusions">
                  <summary class="multi-excl-summary">View Exclusions ▾</summary>
                  <ul class="multi-excl-list">
                    {p.excludes.map(e => <li>✗ {e}</li>)}
                  </ul>
                </details>
              )}
              <div class="multi-actions">
                <a href={`https://wa.me/${wa}?text=Hi%20Shri%20Taxi%2C%20I%27m%20interested%20in%20${encodeURIComponent(p.name)}%20package`} target="_blank" rel="noopener" class="btn btn-whatsapp pkg-btn">💬 Book Now</a>
                <a href={`tel:${phone}`} class="btn btn-outline pkg-btn">📞 Call Us</a>
              </div>
            </div>
          ))}
        </div>
      </div>
    </details>

      </div>
    </section>

    <!-- Why Book -->
    <section class="section">
      <div class="container">
        <h2 class="section-title">Why Book Your Uttarakhand Tour with Shri Taxi?</h2>
        <div class="why-grid">
          {[
            { icon: '💰', title: '10% Discount', desc: 'Get 10% off on all tour packages. Save up to ₹8,991 on Char Dham Yatra.' },
            { icon: '✓', title: 'All Inclusive', desc: 'Driver, fuel, toll and parking all included. No surprise charges at the end.' },
            { icon: '🏔️', title: 'Pahadi Drivers', desc: 'Our drivers know every mountain road. Safe, experienced and local.' },
            { icon: '📞', title: '24/7 Support', desc: 'We are available round the clock for any assistance during your trip.' },
            { icon: '🚗', title: 'Premium Fleet', desc: 'Choose from Sedan, Ertiga, Innova Crysta and Tempo Traveller.' },
            { icon: '⚡', title: 'Instant Booking', desc: 'Confirm your booking via WhatsApp or call. No waiting, no forms.' },
          ].map(w => (
            <div class="why-card">
              <span class="why-icon">{w.icon}</span>
              <h3 class="why-title">{w.title}</h3>
              <p class="why-desc">{w.desc}</p>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="section pkg-cta">
      <div class="container pkg-cta-inner">
        <div>
          <h2>Ready to Book Your Uttarakhand Tour?</h2>
          <p>Get 10% discount on all packages. All inclusive pricing. No hidden charges.</p>
        </div>
        <div class="pkg-cta-btns">
          <a href={`tel:${phone}`} class="btn btn-call">📞 {phone}</a>
          <a href={`https://wa.me/${wa}?text=Hi%20Shri%20Taxi%2C%20I%20want%20to%20book%20a%20tour%20package`} target="_blank" rel="noopener" class="btn btn-whatsapp">💬 WhatsApp Now</a>
        </div>
      </div>
    </section>

  </main>

  <Footer />
  <StickyBar />
</Base>

<script>
  // Chip filter — show/hide accordions
  const chips = document.querySelectorAll('.pkg-chip');
  const accordions = document.querySelectorAll('.pkg-accordion');

  chips.forEach(chip => {
    chip.addEventListener('click', () => {
      const filter = (chip as HTMLElement).dataset.filter;

      // Update active chip
      chips.forEach(c => c.classList.remove('pkg-chip--active'));
      chip.classList.add('pkg-chip--active');

      // Show/hide accordions
      accordions.forEach(acc => {
        const cat = (acc as HTMLElement).dataset.category;
        if (filter === 'all' || filter === cat) {
          (acc as HTMLDetailsElement).style.display = '';
          // Auto-open selected category
          if (filter === cat) (acc as HTMLDetailsElement).open = true;
        } else {
          (acc as HTMLDetailsElement).style.display = 'none';
        }
      });

      // Scroll to first visible accordion
      const firstVisible = document.querySelector('.pkg-accordion:not([style*="none"])');
      firstVisible?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  });
</script>

<style>
/* ── Chip Filter ── */
.pkg-filter-bar {
  background: var(--white);
  border-bottom: 2px solid var(--border);
  padding: 20px 0;
  position: sticky;
  top: 60px;
  z-index: 90;
  box-shadow: 0 2px 8px rgba(0,0,0,0.06);
}

.pkg-filter-label {
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: var(--muted);
  margin-bottom: 12px;
}

.pkg-chips {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.pkg-chip {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 10px 20px;
  font-size: 14px;
  font-weight: 600;
  color: var(--slate);
  background: var(--mint);
  border: 2px solid var(--border);
  border-radius: 30px;
  cursor: pointer;
  font-family: inherit;
  transition: all 0.15s;
  white-space: nowrap;
}

.pkg-chip:hover {
  border-color: var(--teal);
  color: var(--teal);
}

.pkg-chip--active {
  background: var(--teal);
  border-color: var(--teal);
  color: #fff;
}

.pkg-chip--highlight {
  border-color: var(--saffron);
  color: #c2410c;
  background: #fff7ed;
  position: relative;
}

.pkg-chip--highlight.pkg-chip--active {
  background: var(--saffron);
  border-color: var(--saffron);
  color: #fff;
}

.chip-new {
  background: #ef4444;
  color: #fff;
  font-size: 9px;
  font-weight: 800;
  padding: 1px 6px;
  border-radius: 20px;
}

/* ── Accordion ── */
.pkg-accordion {
  border-bottom: 2px solid var(--border);
  max-width: 1200px;
}

.pkg-accordion--highlight {
  border-left: 4px solid var(--saffron);
  padding-left: 16px;
}

.pkg-accordion-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24px 0;
  cursor: pointer;
  list-style: none;
  background: var(--white);
  width: 100%;
}

.pkg-accordion-header::-webkit-details-marker { display: none; }

.pkg-accordion-title {
  display: flex;
  align-items: center;
  gap: 16px;
}

.pkg-accordion-icon { font-size: 32px; }

.pkg-accordion-title h2 {
  font-size: 22px;
  font-family: 'Playfair Display', serif;
  color: var(--slate);
  margin-bottom: 4px;
  display: flex;
  align-items: center;
  gap: 10px;
}

.pkg-accordion-title p {
  font-size: 13px;
  color: var(--muted);
}

.pkg-accordion-meta {
  display: flex;
  align-items: center;
  gap: 12px;
  flex-shrink: 0;
}

.pkg-accordion-count {
  background: var(--mint);
  color: var(--teal);
  font-size: 12px;
  font-weight: 700;
  padding: 4px 12px;
  border-radius: 20px;
}

.pkg-accordion-chevron {
  color: var(--muted);
  transition: transform 0.2s;
  flex-shrink: 0;
}

details[open] .pkg-accordion-chevron { transform: rotate(180deg); color: var(--teal); }
details[open] .pkg-accordion-header { color: var(--teal); }

.pkg-accordion-body {
  padding: 8px 0 32px;
  max-width: 1200px;
}

.accordion-new-badge {
  background: #ef4444;
  color: #fff;
  font-size: 11px;
  font-weight: 800;
  padding: 2px 8px;
  border-radius: 20px;
  font-family: 'DM Sans', sans-serif;
  vertical-align: middle;
}

/* Mobile */
@media (max-width: 640px) {
  .pkg-chips { gap: 8px; }
  .pkg-chip { padding: 8px 14px; font-size: 13px; }
  .pkg-accordion-icon { font-size: 24px; }
  .pkg-accordion-title h2 { font-size: 18px; }
  .pkg-accordion-count { display: none; }
}
/* Hero */
.pkg-hero {
  background: linear-gradient(135deg, #0d9488, #0f766e);
  padding: 52px 0 40px;
}
.breadcrumb { font-size: 12px; color: rgba(255,255,255,0.65); margin-bottom: 16px; }
.breadcrumb a { color: rgba(255,255,255,0.75); text-decoration: none; }
.pkg-hero-title { font-size: clamp(26px, 4vw, 42px); color: #fff; font-family: 'Playfair Display', serif; margin-bottom: 10px; }
.pkg-hero-sub { font-size: 14px; color: rgba(255,255,255,0.8); max-width: 600px; line-height: 1.6; margin-bottom: 24px; }
.pkg-hero-ctas { display: flex; gap: 12px; flex-wrap: wrap; }

/* Discount bar */
.discount-bar {
  background: linear-gradient(135deg, #fb923c, #f97316);
  padding: 12px 0;
}
.discount-bar-inner {
  display: flex;
  align-items: center;
  gap: 12px;
  justify-content: center;
}
.discount-fire { font-size: 20px; }
.discount-bar-inner p { font-size: 14px; color: #fff; margin: 0; }
.discount-badge {
  background: #fff;
  color: #f97316;
  font-size: 12px;
  font-weight: 800;
  padding: 3px 12px;
  border-radius: 20px;
  flex-shrink: 0;
}

/* Package grid */
.pkg-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 380px));
  gap: 20px;
}

/* Package card */
.pkg-card {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  overflow: hidden;
  display: flex;
  flex-direction: column;
  transition: box-shadow 0.2s, transform 0.2s;
  width: 100%;
}
.pkg-card:hover { box-shadow: var(--shadow-md); transform: translateY(-4px); }

/* Image */
.pkg-img-wrap {
  position: relative;
  height: 200px;
  overflow: hidden;
}
.pkg-img { width: 100%; height: 100%; object-fit: cover; display: block; transition: transform 0.35s; }
.pkg-card:hover .pkg-img { transform: scale(1.06); }
.pkg-overlay { position: absolute; inset: 0; background: linear-gradient(to top, rgba(0,0,0,0.5) 0%, transparent 60%); }
.pkg-badge { position: absolute; top: 10px; left: 10px; color: #fff; font-size: 10px; font-weight: 700; padding: 3px 9px; border-radius: 20px; z-index: 1; }
.pkg-nights { position: absolute; bottom: 10px; left: 10px; background: rgba(0,0,0,0.6); color: #fff; font-size: 11px; font-weight: 600; padding: 3px 9px; border-radius: 20px; z-index: 1; }

/* Discount circle */
.pkg-discount-circle {
  position: absolute;
  top: 10px;
  right: 10px;
  background: #ef4444;
  color: #fff;
  border-radius: 50%;
  width: 44px;
  height: 44px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  z-index: 2;
  animation: pulse-badge 2s ease-in-out infinite;
  line-height: 1.1;
}
.pkg-discount-circle span { font-size: 10px; font-weight: 800; }
@keyframes pulse-badge { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.1); } }

/* Body */
.pkg-body { padding: 16px; display: flex; flex-direction: column; gap: 10px; flex: 1; }
.pkg-name { font-size: 16px; font-weight: 700; color: var(--slate); }

.pkg-highlights { display: flex; flex-wrap: wrap; gap: 4px; }
.pkg-highlight-chip {
  background: var(--mint);
  color: var(--teal);
  font-size: 10px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: 20px;
  border: 1px solid rgba(13,148,136,0.2);
}

/* Pricing table */
.pkg-pricing-table {
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  overflow: hidden;
}

.pkg-pricing-header {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr 1fr;
  background: var(--teal);
  color: #fff;
  padding: 6px 10px;
  font-size: 10px;
  font-weight: 600;
  text-transform: uppercase;
}

.pkg-pricing-row {
  display: grid;
  grid-template-columns: 2fr 1fr 1fr 1fr;
  padding: 8px 10px;
  border-bottom: 1px solid var(--border);
  align-items: center;
  transition: background 0.15s;
}
.pkg-pricing-row:last-child { border-bottom: none; }
.pkg-pricing-row:hover { background: var(--mint); }

.pkg-tier-vehicle { font-size: 12px; font-weight: 600; color: var(--slate); }
.pkg-tier-mrp { font-size: 11px; color: var(--muted); text-decoration: line-through; }
.pkg-tier-final { font-size: 13px; font-weight: 700; color: var(--teal); }
.pkg-tier-save { font-size: 10px; font-weight: 600; color: #22c55e; }

.pkg-inclusive { font-size: 11px; color: var(--teal); font-weight: 500; }

.pkg-actions { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin-top: auto; }
.pkg-btn { justify-content: center; padding: 9px 8px; font-size: 12px; }

/* Char Dham no-image cards */
.chardham-grid { display: flex; flex-direction: column; gap: 20px; }

.chardham-card {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 24px 28px;
  transition: box-shadow 0.2s;
}
.chardham-card:hover { box-shadow: var(--shadow-md); }

.chardham-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 14px; }
.chardham-header-left { display: flex; align-items: flex-start; gap: 14px; }
.chardham-icon { font-size: 28px; line-height: 1; padding-top: 4px; flex-shrink: 0; }
.chardham-tag { font-size: 11px; font-weight: 700; color: #fff; padding: 3px 10px; border-radius: 20px; display: inline-block; margin-bottom: 6px; }
.chardham-name { font-size: 20px; font-weight: 700; color: var(--slate); font-family: 'Playfair Display', serif; margin-bottom: 4px; }
.chardham-sub { font-size: 13px; color: var(--muted); }
.chardham-discount-badge { background: #ef4444; color: #fff; font-size: 13px; font-weight: 800; padding: 6px 14px; border-radius: 20px; flex-shrink: 0; animation: pulse-badge 2s ease-in-out infinite; }

.chardham-itinerary { display: grid; grid-template-columns: repeat(2, 1fr); gap: 6px; margin-bottom: 16px; background: var(--mint); border-radius: var(--radius-md); padding: 12px; }
.itinerary-day { font-size: 12px; color: var(--muted); }

.chardham-pricing-table { display: grid; grid-template-columns: repeat(2, 1fr); gap: 10px; margin-bottom: 12px; }
.chardham-tier { background: var(--mint); border: 1px solid rgba(13,148,136,0.2); border-radius: var(--radius-md); padding: 12px; display: flex; flex-direction: column; gap: 6px; }
.chardham-tier-vehicle { font-size: 13px; font-weight: 600; color: var(--slate); display: flex; align-items: center; gap: 6px; flex-wrap: wrap; }
.chardham-seats { font-size: 11px; color: var(--muted); font-weight: 400; }
.chardham-tier-price { display: flex; align-items: baseline; gap: 8px; flex-wrap: wrap; }
.chardham-mrp { font-size: 12px; color: var(--muted); text-decoration: line-through; }
.chardham-final { font-size: 20px; font-weight: 800; color: var(--teal); font-family: 'Playfair Display', serif; }
.chardham-save { font-size: 11px; color: #22c55e; font-weight: 700; background: #f0fdf4; padding: 2px 8px; border-radius: 20px; }

/* Why grid */
.pkg-filter-tab--active {
  color: var(--teal);
  border-bottom-color: var(--teal);
  background: var(--mint);
}

/* ── Quick Filter Tabs ── */
.pkg-filter-bar {
  background: var(--mint);
  border-bottom: 2px solid var(--border);
  padding: 20px 0;
}

.pkg-filter-label {
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
  color: var(--muted);
  margin-bottom: 12px;
}

.pkg-filter-tabs {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
}

.pkg-filter-tab {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 18px 16px;
  font-size: 15px;
  font-weight: 700;
  color: var(--slate);
  text-decoration: none;
  background: var(--white);
  border: 2px solid var(--border);
  border-radius: var(--radius-lg);
  text-align: center;
  transition: all 0.2s;
  position: relative;
}

.pkg-filter-tab:hover {
  border-color: var(--teal);
  color: var(--teal);
  box-shadow: var(--shadow-md);
  transform: translateY(-2px);
}

.pkg-filter-icon { font-size: 28px; }
.pkg-filter-text { font-size: 14px; font-weight: 700; line-height: 1.3; }

.pkg-filter-tab--highlight {
  border-color: var(--saffron);
  background: linear-gradient(135deg, #fff7ed, #fff3e0);
  color: #c2410c;
}

.pkg-filter-tab--highlight:hover {
  border-color: #ea580c;
  color: #9a3412;
  background: linear-gradient(135deg, #ffedd5, #fed7aa);
}

.pkg-filter-new {
  position: absolute;
  top: -10px;
  right: 12px;
  background: #ef4444;
  color: #fff;
  font-size: 10px;
  font-weight: 800;
  padding: 2px 8px;
  border-radius: 20px;
  letter-spacing: 0.5px;
}

.pkg-filter-tab--active {
  border-color: var(--teal);
  color: var(--teal);
  background: rgba(13,148,136,0.06);
}

/* Mobile — stack vertically */
@media (max-width: 640px) {
  .pkg-filter-tabs {
    grid-template-columns: 1fr;
    gap: 10px;
  }
  .pkg-filter-tab {
    flex-direction: row;
    justify-content: flex-start;
    padding: 14px 16px;
    gap: 12px;
  }
  .pkg-filter-icon { font-size: 24px; }
  .pkg-filter-text { font-size: 15px; text-align: left; }
}

/* ── Multi-Destination ── */
.multi-notice { display: flex; align-items: center; gap: 10px; background: var(--mint); border: 1px solid rgba(13,148,136,0.2); border-radius: var(--radius-md); padding: 12px 16px; margin-bottom: 12px; font-size: 13px; color: var(--slate); }
.cab-only-notice { display: flex; align-items: center; gap: 10px; background: #fffbf0; border: 1.5px solid #fcd34d; border-radius: var(--radius-md); padding: 12px 16px; margin-bottom: 24px; font-size: 13px; color: var(--slate); }
.multi-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 24px; }
.multi-card { background: var(--white); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 24px; display: flex; flex-direction: column; gap: 14px; }
.multi-header { display: flex; justify-content: space-between; align-items: flex-start; }
.multi-tag { font-size: 11px; font-weight: 700; color: #fff; padding: 3px 10px; border-radius: 20px; display: inline-block; margin-bottom: 6px; }
.multi-name { font-size: 18px; font-weight: 700; color: var(--slate); font-family: 'Playfair Display', serif; margin-bottom: 4px; }
.multi-duration { font-size: 13px; color: var(--muted); }
.multi-discount { background: #ef4444; color: #fff; font-size: 13px; font-weight: 800; padding: 6px 14px; border-radius: 20px; flex-shrink: 0; animation: pulse-badge 2s ease-in-out infinite; }
.multi-highlights { display: flex; flex-wrap: wrap; gap: 6px; }
.multi-highlight { background: var(--mint); color: var(--teal); font-size: 11px; font-weight: 600; padding: 3px 10px; border-radius: 20px; border: 1px solid rgba(13,148,136,0.2); }
.multi-itinerary { display: grid; grid-template-columns: repeat(2, 1fr); gap: 6px; background: var(--mint); border-radius: var(--radius-md); padding: 12px; }
.multi-day { font-size: 12px; color: var(--muted); }
.multi-pricing-table { display: grid; grid-template-columns: repeat(2, 1fr); gap: 8px; }
.multi-tier { background: var(--mint); border: 1px solid rgba(13,148,136,0.2); border-radius: var(--radius-md); padding: 10px 12px; display: flex; flex-direction: column; gap: 4px; }
.multi-tier-vehicle { font-size: 12px; font-weight: 600; color: var(--slate); display: flex; align-items: center; gap: 4px; flex-wrap: wrap; }
.multi-tier-seats { font-size: 10px; color: var(--muted); font-weight: 400; }
.multi-tier-price { display: flex; align-items: baseline; gap: 6px; flex-wrap: wrap; }
.multi-mrp { font-size: 11px; color: var(--muted); text-decoration: line-through; }
.multi-final { font-size: 18px; font-weight: 800; color: var(--teal); font-family: 'Playfair Display', serif; }
.multi-save { font-size: 10px; color: #22c55e; font-weight: 700; background: #f0fdf4; padding: 2px 6px; border-radius: 20px; }
.multi-inclusive { font-size: 11px; color: var(--teal); font-weight: 500; }
.multi-exclusions { border: 1px solid var(--border); border-radius: var(--radius-md); overflow: hidden; }
.multi-excl-summary { padding: 10px 14px; font-size: 13px; font-weight: 600; color: var(--muted); cursor: pointer; list-style: none; background: #fafafa; }
.multi-excl-summary::-webkit-details-marker { display: none; }
details[open] .multi-excl-summary { color: var(--slate); background: #f5f5f5; }
.multi-excl-list { list-style: none; padding: 12px 14px; display: flex; flex-direction: column; gap: 6px; }
.multi-excl-list li { font-size: 12px; color: var(--muted); }
.multi-actions { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin-top: auto; }

/* ── Why grid ── */
.why-card { background: var(--white); border: 1px solid var(--border); border-radius: var(--radius-lg); padding: 24px; text-align: center; }
.why-icon { font-size: 32px; display: block; margin-bottom: 10px; }
.why-title { font-size: 16px; font-weight: 700; color: var(--teal); margin-bottom: 8px; }
.why-desc { font-size: 13px; color: var(--muted); line-height: 1.6; }

/* CTA */
.pkg-cta { background: var(--teal); }
.pkg-cta-inner { display: flex; justify-content: space-between; align-items: center; gap: 24px; flex-wrap: wrap; }
.pkg-cta h2 { font-size: 24px; color: #fff; font-family: 'Playfair Display', serif; margin-bottom: 6px; }
.pkg-cta p { font-size: 14px; color: rgba(255,255,255,0.75); }
.pkg-cta-btns { display: flex; gap: 10px; flex-wrap: wrap; }

/* Responsive */
@media (max-width: 1100px) {
  .chardham-card { grid-template-columns: 300px 1fr; }
}
@media (max-width: 900px) {
  .pkg-grid { grid-template-columns: repeat(2, 1fr); }
  .why-grid { grid-template-columns: repeat(2, 1fr); }
  .multi-grid { grid-template-columns: 1fr; }
  .chardham-card { grid-template-columns: 1fr; }
  .chardham-img-wrap { height: 220px; }
  .chardham-body { grid-template-columns: 1fr; }
  .chardham-info { border-right: none; border-bottom: 1px solid var(--border); }
}
@media (max-width: 600px) {
  .pkg-grid { grid-template-columns: 1fr; }
  .why-grid { grid-template-columns: 1fr; }
  .pkg-hero-ctas { flex-direction: column; }
  .discount-bar-inner { flex-direction: column; text-align: center; }
  .pkg-cta-inner { flex-direction: column; }
  .pkg-cta-btns { width: 100%; flex-direction: column; }
  .pkg-cta-btns .btn { justify-content: center; }
}
</style>
